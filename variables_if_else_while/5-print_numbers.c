#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main- prints all single digit numbers
 *
 * Return: Always (0)
 */
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}

